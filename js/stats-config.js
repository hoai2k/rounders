/**
 * Where the stats collector lives. One line to fill in after deploying the
 * worker in `worker/` — see STATS.md.
 *
 * Left empty, the game sends nothing at all and the dashboard at /stats/
 * explains how to switch it on, so a fresh clone stays offline by default.
 */
window.ROUNDERS_STATS = {
  endpoint: "" // e.g. "https://rounders-stats.your-name.workers.dev"
};
